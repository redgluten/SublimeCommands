<snippet>
    <content><![CDATA[
\$${1:name} = \$${2:model}->${1:name};
\$${2:Model}->destroy();

flash('The ${2:model} ' . \$${1:name} . ' was deleted.');

return redirect()->route('${2:model}s.index');
]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>laradestroy</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <scope>source.php</scope>
</snippet>
