<snippet>
    <content><![CDATA[
\$${1:models} = ${2:Model}::${3:orderBy('created_at', 'desc')->}paginate(${4:10});

return view('${1:models}.index', compact('${1:models}'));
]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>laraindex</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <scope>source.php</scope>
</snippet>
