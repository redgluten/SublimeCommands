<snippet>
    <content><![CDATA[
\$${1:modelName} = \$request->persist(\$${1:modelName});

flash('The ${1:modelName} ' . \$${1:modelName}->${2:name} . ' has been updated');

return redirect()->route('${1:modelName}s.show', \$${1:modelName}->id);
]]></content>
    <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
    <tabTrigger>laraupdate</tabTrigger>
    <!-- Optional: Set a scope to limit where the snippet will trigger -->
    <scope>source.php</scope>
</snippet>
